<?php

//namespace App\Http\Middleware;

//use Closure;
//use Illuminate\Http\Request;

//class MediaPermissionMiddleware
//{
  //  public function handle(Request $request, Closure $next)
    //{
        // Check if media permissions are already granted (can be stored in the session or localStorage)
//         if (!$request->session()->has('media_permission_granted')) {
//             return redirect('/media-permission');
//         }

//         return $next($request);
//     }
// }
